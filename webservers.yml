---

- name: configure the webservers
  hosts:
    - spf
    - webservers
  sudo: yes

  roles:
    - common
    - webservers
    - composer
    - mariadb
  handlers:
    - include: handlers/nginx.yml
