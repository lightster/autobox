---

- name: configure the webservers
  hosts:
    - spf
    - webservers
  sudo: yes

  roles:
    - common
    - webservers
    - composer
    - mailgun
  handlers:
    - include: handlers/nginx.yml
